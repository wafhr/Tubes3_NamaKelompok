{
  "name": "judol-detector",
  "version": "0.1.0",
  "private": true,
  "description": "Chromium browser extension untuk mendeteksi konten judi online dengan pattern matching.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build --config vite.content.config.ts && vite build --config vite.popup.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.302",
    "@types/node": "^22.15.19",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
